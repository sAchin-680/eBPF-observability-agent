apiVersion: 1

providers:
  - name: ebpf-agent
    type: file
    # Dashboards are provisioned from files under version control rather than
    # created in the UI, so that what is deployed is what is reviewed.
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
